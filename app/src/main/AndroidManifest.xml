<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nimashi.worky">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CountPageThreeActivity"></activity>
        <activity android:name=".CountPageTwoActivity" /> <!-- <activity android:name=".ProfileActivity"></activity> -->
        <activity android:name=".ReportActivity" />
        <activity android:name=".CpageActivity" />
        <activity android:name=".CountPageActivity" />
        <activity android:name=".CounterActivity" />
        <activity android:name=".ExMainActivity" />
        <activity android:name=".GenderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>